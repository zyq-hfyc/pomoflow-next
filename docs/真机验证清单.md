# 真机验证清单(统一汇总 · 更新 2026-09-03)

> **用途**:Windows 主机只能跑单测/analyze/Web 预览,mobile 的 native 链路
> (通知/后台/同步)一直没在真设备上端到端跑过。本清单汇总**所有待真机
> 验证项**(来源:`协作任务清单.md` 各批次的 × 真机验证条目,
> 2026-08-25 → 2026-09-03),按验证会话分组 —— **A 组装机即可验、
> B 组需要服务端 + 桌面、C 组桌面自验**,建议按 A → B → C 一条龙。
> 2026-09-02 批次的细粒度操作步骤见
> [`2026-09-02真机测试清单.md`](./2026-09-02真机测试清单.md)。

## 0. 前置准备(一次性,约 10 分钟)

| 项 | 操作 | 预期 |
|----|------|------|
| APK 构建 | 见 [`B-Phase-3 runbook` §1](./B-Phase-3安卓真机验证runbook.md) | `app-debug.apk` 产出 |
| USB 调试 | 手机开发者模式 + USB 调试 + 授权 | `adb devices` 显示 `device` |
| 装机 | `adb install -r apps/mobile/build/app/outputs/flutter-apk/app-debug.apk` | `Success` |
| 通知权限 | 首次点专注页铃铛 → 允许(A/B/C 组的通知项都依赖) | 铃铛 🔔 |
| 服务端(B/C 组) | VM 上 sync-server 运行中 | `curl http://<vm-ip>:8080/healthz` → `{"ok":true}` |
| 桌面端(B/C 组) | 同一账号已登录(用于双向验证) | 桌面任务列表可操作 |

> 手机与 VM 同一局域网;App 登录页服务器地址填 `http://<vm-ip>:8080`。

## A. 纯手机本地功能(不依赖服务端,装机即可验)

### A1 设置与计时(2026-08-31 设置批)

- [ ] 设置改专注/短休/长休时长 → 专注页生效(修改不重置进行中倒计时,
      归零/切换/跳过后重取新值)
- [ ] 任务级「单番茄时长」覆盖全局(表单设 40 → 专注页按 40 计)

### A2 计时结束系统通知(2026-09-02 通知批)

- [ ] 专注走完 → 系统通知弹出,文案用模板正文 + `{task_title}` 替换
- [ ] 短/长休息走完 → 各自文案弹出;休息结束**不**计今日番茄
- 详细步骤(权限拒绝/持久化/锁屏/杀 app 重开 10 项)见
  [`2026-09-02清单 §一`](./2026-09-02真机测试清单.md)

### A3 通知文案模板(2026-09-02 Batch5-3 + 2026-09-03 reminder 档)

- [ ] 设置 → 通知文案:sheet 正常弹出(**回归 Bug1:曾只剩透明蒙层**)
- [ ] 切 6 套预设 → 8 个输入框整套替换;单独改「任务提醒」标题/正文 →
      保存 → 重进仍在;下一次计时/提醒通知用新文案
- [ ] 正文含 `{task_title}` 时按实际任务名替换

### A4 任务提醒触发引擎(2026-09-03,最新批)

> 引擎前台 30s 一查,通知在提醒时间点后 ≤30s 内到达属正常。

- [ ] 建任务:到期 = 当前 +3 分钟,提醒 = 准时 → 保存后停在 app 内等
      → 到点弹「任务提醒 📌 「任务名」提醒时间已到」
- [ ] 提前量档位抽查:提醒 = 提前 5 分钟 → 到期前 5 分钟弹
      (提醒时间点 = 到期日 − 提前量)
- [ ] 专注抑制:开始一个专注 → 提醒到点**不弹** → 暂停 → 30s 内补弹
- [ ] 去重:同一条提醒只弹一次(到点后再等 1 分钟/切后台回前台不重弹)
- [ ] 错过补弹:杀 app → 到点后重新打开 → 回前台立即补弹
- [ ] 开关:专注页铃铛关(🔕)→ 到点静默不弹

### A5 任务表单与视图(2026-09-03 Bug 修复批)

- [ ] 编辑任务:到期日 = 日期+时间双 picker,可清除(**回归:曾是
      纯日期「截止日期」**);设了提醒必须先选到期日(联动校验提示)
- [ ] 重复-自定义:周类型选星期几 / 月类型选每月几号 / 频率 1-100 /
      起止时间(结束需晚于开始)→ 保存 → 详情显示「自定义」
- [ ] 详情 sheet:到期日 yyyy-MM-dd HH:mm / 提醒中文档位 / 重复标签正确
- [ ] 「今天/明天」视图按到期日本地日分流;无到期日任务进「计划」
- [ ] (Bug1 修复回归)新建任务 → 所属项目下拉弹菜单可切换;「管理」→
      项目管理 sheet 正常显示列表/新建顶级/长按改父(曾透明蒙层卡死)

### A6 搜索/筛选/统计联动(2026-08-31 搜索批)

- [ ] 搜索条实时过滤(标题/项目/标签模糊匹配)
- [ ] 项目/优先级/标签筛选 → 4 统计卡(任务数/番茄/预计/已专注分钟)即时联动

### A7 统计 CSV 导出(2026-09-02)

- [ ] 统计页 → ⤓ → 系统分享面板弹出 CSV(UTF-8 中文不乱码,三段结构)
- 详细步骤 6 项见 [`2026-09-02清单 §二`](./2026-09-02真机测试清单.md)

### A8 帮助与反馈(2026-09-02 Batch5-5)

- [ ] 我的 → 帮助与反馈 → 用户手册/FAQ/联系我们三段 Tab 内容完整可读

### A9 周/月复盘本地持久化(2026-09-02)

- [ ] 复盘页日/周/月三栏切换、输入自动保存、重进仍在
- 详细步骤 3.1-3.6 见 [`2026-09-02清单 §三`](./2026-09-02真机测试清单.md)

### A10 重复任务实例引擎(2026-09-03,最新批)

> 实例 = 模板按规则展开的未来任务;到当年 12-31 / custom 到配置 endDate,
> 50 条上限。语义与桌面端一致(core repeat.rs 对拍单测已锁,真机验 UI 链路)。

- [ ] 建模板:到期日设明天,重复 = 每天 → 保存后任务页出现多条同标题
      实例(从后天起逐日;「今天」视图只看到期当天的)
- [ ] 实例重置语义:实例无重复标签、番茄进度 0、勾完成只影响这一条
      (模板和其他实例不动)
- [ ] 模板编辑重排:每天 → 每周 → 未完成实例消失重排为每周;**已完成的
      实例保留**;改到期日/自定义配置同样触发
- [ ] 自定义规则:周几多选(如一/三/五)→ 实例只落这些天
- [ ] 删模板 → 全部实例(含已完成)一并消失(进回收站)
- [ ] 回收站恢复模板 → 实例不回来(桌面同语义,恢复只还原模板本体)

### A11 手账编辑/删除(2026-09-03 P3g 批)

- [ ] 任务页手账区点任意卡片 → 编辑 sheet 弹出(预填标题/内容/标签,
      顶部四类 chips 当前类高亮)
- [ ] 切换四类(如 待办→愿望)+ 改标题/内容/标签 → 保存 → 列表卡片
      即时变新类新标题;杀 app 重进仍在
- [ ] 标题和内容都清空 → 保存 → 提示「标题和内容至少填一项」,不落库
- [ ] 删除 → 二次确认弹窗 → 确认后卡片消失;重进不复活
- [ ] (P3h)四类筛选 chips:切「⭐ 愿望」→ 只剩愿望条目,4 统计卡
      显示各类条数且随筛选联动;「全部」恢复
- [ ] (P3h)🏷 标签筛选:选某标签 → 只剩带该标签手账;与 kind 筛选
      叠加(交集);搜索框输内容关键词能搜到空标题小记

## B. 手机 ↔ 桌面跨端同步(需 §0 服务端 + 桌面登录)

### B1 登录与账号(2026-08-25 P3d 批)

- [ ] 登录成功进入首页;我的页用户名/邮箱/头像字母占位
- [ ] 头像上传:相册选图 → 圆形头像,重进保留
- [ ] 数据导出:分享面板弹出 JSON 备份
- [ ] 登录设备列表出现手机设备名;退出登录 → 重登数据仍在

### B2 任务同步双向闭环(2026-08-25 P1 批)

- [ ] 桌面建任务 → 手机立即同步出现;手机勾完成 → 桌面变已完成;
      手机编辑标题 → 同步后桌面更新
- [ ] 服务端核对(可选):snapshots 出现 `entity='task'` 且 device_id
      为手机名的行

### B3 任务字段级跨端(多批收拢)

- [ ] 表单时长/重复(2026-08-31):手机设时长+重复 → 桌面正确显示
- [ ] 重复 6 规则(2026-09-02):weekly/monthly/yearly/weekdays 规则建
      任务 → 桌面正确显示
- [ ] 到期日/提醒/自定义重复(2026-09-03):手机设到期日+提醒+自定义
      规则 → 桌面正确显示;桌面改 → 手机同步回正确

### B4 软删除 + 回收站(2026-08-31 P2 回收站批)

- [x] 本地链路:user 已验证(2026-09-01 批注:回收站聚类 fabce3e 通过)
- [ ] 恢复:回收站恢复 → 回列表且同步到桌面重新出现
- [ ] 彻底删除不复活;删除当前专注任务 → 专注屏解除关联
- [ ] 桌面删 → 手机立即同步消失;两边各同步 2 轮不复活

### B5 专注 session + 计数口径(2026-08-25 P1 + 2026-08-31 completed_at 批)

- [ ] 手机专注走完 → 今日番茄 +1;推 session 行(`entity='pomodoro_session'`)
- [ ] 桌面统计页能看到手机产生的番茄;两端同日番茄数一致
- [ ] 勾完成任务的「完成任务」区间计数正确
- [ ] 跳过专注 ≥1 分钟 → 历史会话落库(不计番茄、可查、随同步保留)

### B6 统计页真数据(2026-08-25 P2 批)

- [ ] 六维度切换数字即时变化;4 统计卡与实际记录一致
- [ ] 同步拉下桌面 session 后**不重启**切统计页数字即更新
- [ ] 不选任务直接专注走完 → 今日番茄不 +1(口径对齐桌面)

### B7 实体跨端:project / tags / subtask(2026-09-01)

- [x] project 基础两向:装机后未反馈问题(默认通过,日常使用继续观察)
- [ ] 服务端核对(可选):snapshots 出现 `entity='project'` 行
- [ ] tags:手机任务打标签 → 桌面看到;桌面标签变更 → 手机 tags_csv/
      筛选联动;服务端 `entity='tag'`/`'task_tag'` 行
- [ ] subtask:手机加子任务 → 桌面勾选联动;桌面子任务 → 手机详情可见
      (父任务 subtask_cnt 实时更新)

### B8 description / 今日复盘 / 座右铭(2026-09-01 P0 批)

- [ ] **P0 回归**:桌面写任务描述 → 手机编辑其他字段同步 → 描述保留
      (不做就重复数据丢失事故)
- [ ] 手机写今日复盘 → 桌面看到;桌面座右铭 → 手机专注页轮播

### B9 周/月复盘 + 冲突记录双端(2026-09-02)

- [ ] 手机写周复盘 → 桌面手账页可见(反向同)
- [ ] 两端改同一任务(各自断网改再联网)→ 桌面设置「同步记录」出现条目
      + 手机我的页「同步记录」出现 1 条 overrode 记录
- 详细步骤见 [`2026-09-02清单 §三 3.7-3.9 / §四 / §五`](./2026-09-02真机测试清单.md)

### B10 项目层级(2026-09-02 Batch5-2)

- [ ] 移动端新建子项目(长按改父)→ 桌面清单管理可见层级(深度 ≤3)
- [ ] 手机任务选子项目 → 桌面归属正确

### B11 后台自动同步(2026-08-25 workmanager 批)

**MIUI 前置(必做,否则周期任务被系统杀)**:

- [ ] 设置 → 应用管理 → PomoFlow → 省电策略 → **无限制**
- [ ] 手机管家 → 权限 → 自启动 → PomoFlow **开启**

**验证**:

- [ ] 打开「自动同步」→ 桌面建任务 → 手机锁屏 30 分钟后解锁 → 任务自动出现
- [ ] 关闭开关 → 不再后台同步
- [ ] (排查)`adb logcat -s flutter` 后台到点出现 `[bg-sync] 已同步 · …`
      (现在该周期还会带 `[bg-reminder]` 提醒兜底检查日志)

### B12 异常路径(可选)

- [ ] 断网点「立即同步」→ 失败提示,本地操作不受影响;恢复网络补推不丢
- [ ] 服务器地址配错 → 登录页报错不崩溃

### B13 重复实例跨端(2026-09-03 实例引擎批)

> 编排只在创建/编辑模板的设备上发生,另一端靠同步收「模板 + 实例」
> —— 两端同时在线改规则也不会生成两套。

- [ ] 桌面建重复任务 → 手机同步收到模板 + 实例(条数与桌面一致,
      **手机侧不重复展开**)
- [ ] **P0 回归**:手机编辑桌面生成的实例(改标题/勾完成)→ 同步后
      桌面这条仍是实例、模板链接不断(此前 mobile 编辑会把
      repeat_parent_id 冲成 null,同 description 事故家族)
- [ ] 手机建重复模板 → 桌面收到模板 + 实例;手机改规则(每天→每周)
      → 桌面实例同样重排、已完成保留
- [ ] 任一端删模板 → 另一端实例全部消失,同步 2 轮不复活

### B14 手账跨端 + 数据晋升(2026-09-03 P3f 批)

> **前置(必做)**:VM 上 sync-server 用新二进制重部署
> (`bash services/sync-server/pack-deploy.sh` 后按 README 换包重启;
> 旧二进制的 serde 不识 journal,push 会 400)。健康检查
> `curl http://<vm-ip>:8080/healthz` 通过后再验。

- [ ] 手机任务页 → 快速新建待办/愿望/年度规划/小记(各 1 条)→ 立即同步
      → 服务端 snapshots 出现 `entity='journal'` 行(可选核对)
- [ ] 桌面设置 → 同步记录:journal 条目标题显示正常(空标题小记显示
      content 首段);桌面「随手记」视图可见手机写的手账(P3i 起有 UI,
      不再需要查库)
- [ ] **数据晋升**:升级前写的老手账(升级 App 后)首次同步自动上云
      → 我的页「数据导出」JSON 备份里出现 journal 条目
- [ ] 两端各写一条 → 互不覆盖;重登账号 → 手账从服务端恢复
- [ ] (P3g)手机编辑手账(换类/改标题)→ 同步后服务端 snapshots
      journal 行 revision 上升、payload 带新值;手机删除 → 另一端
      同步 2 轮不复活(墓碑收敛)

## C. 桌面端功能(桌面自验,不占手机会话)

### C1 垃圾箱 UI(2026-09-02 Batch5-4)

- [ ] 桌面删任务 → 设置 → 垃圾箱:列表/还原/彻底删除/一键清空;
      中英双语文案完整

### C2 桌面「同步记录」(2026-09-02 冲突日志批)

- [ ] 设置 → 数据同步 → 同步记录区:刷新/清空/列表;无冲突时空态提示
- 详细步骤见 [`2026-09-02清单 §五`](./2026-09-02真机测试清单.md)

### C3 桌面「随手记」(2026-09-03 P3i,journal 实体桌面读写)

> 任务页侧栏第 7 视图「随手记」= 移动端「手账」,同一 journal 实体。

- [ ] 切「随手记」→ 四类筛选 chips 带条数、切换即时过滤;空态提示正常
- [ ] 新建四类各一条(空标题纯内容的小计可存)→ 列表即时出现;编辑
      (换类/改标题/标签)→ 保存即变;删除两步确认 → 消失
- [ ] 手机写一条手账 → 立即同步 → 桌面随手记不重启即出现(反向:
      桌面写 → 手机手账视图出现)

## D. 待决事项

- [ ] **release APK 切换**(user 决定时机):卸 debug 前**先立即同步**;
      卸载装 release(arm64 18.5MB)→ 重登,数据从服务端恢复
      (keystore 已 gitignore,密码 user 自保管)

## 故障排查

见 [`B-Phase-3 runbook` §5](./B-Phase-3安卓真机验证runbook.md)(adb 不识别 /
登录失败 / 同步 0-0 / cleartext / gradle 卡下载 / JDK 版本)。

## 验证完成后

- [ ] 勾掉本清单各节,并同步勾掉 `docs/协作任务清单.md` 里对应批次的
      「× 真机验证」条目
- [ ] 发现的缺陷记入协作任务清单新批次(附复现步骤 + 现象 + 时间点 +
      `adb logcat -s flutter` 输出)
